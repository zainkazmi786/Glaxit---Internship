{
  "name": "lecture1",
  "version": "1.0.0",
  "description": "This is a simple user authentication system using ** Node.js**  and the **`fs` module** for data storage. User credentials are stored in a local `users.json` file.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "express": "^5.1.0"
  }
}
